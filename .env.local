# Local Development Configuration (without Docker)
DATABASE_URL=mongodb://localhost:27017/store_api

# MongoDB Configuration for local development
MONGODB_REPLICA_SET_MODE=primary
MONGODB_ADVERTISED_HOSTNAME=localhost
ALLOW_EMPTY_PASSWORD=yes

# Application Configuration for local development
APP_HOST=127.0.0.1
APP_PORT=8000
APP_RELOAD=true

# Local Configuration
COMPOSE_PROJECT_NAME=store-api-local
